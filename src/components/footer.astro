---
import { mainNav } from '../data/nav';
---

<footer class="bg-primary p-10">
  <div class="container mx-auto">
    <div class="flex flex-col">
      <div class="flex items-start justify-between">
        <div>
          <h3 class="text-white font-bold uppercase">
            Hospital Central de los Altos
          </h3>
          <ul class="flex flex-col gap-2">
            {
              mainNav.map((item) => {
                const { href, label } = item;

                return (
                  <li class="font-base text-tertiary ">
                    <a href={href}>{label}</a>
                  </li>
                );
              })
            }
          </ul>
        </div>
        <div class="flex flex-col items-end gap-10">
          <p class="text-white font-base text-right max-w-[40ch]">
            Solicita información. Déjanos tu correo electrónico y alguien se
            pondrá en contacto contigo.
          </p>
          <div class="flex justify-end gap-5">
            <input
              type="text"
              class="bg-white min-w-[400px] rounded-lg p-2"
              placeholder="you@example.com"
            />
            <button class="px-8 py-2 bg-secondary rounded-lg text-white"
              >Enviar</button
            >
          </div>
        </div>
      </div>
      <div class="mt-2 flex flex-col gap-2 font-base text-tertiary">
        <p>Bartolo Hernández #250, Tepatitlán de Morelos, Jalisco.</p>
        <a href="">378 122 0320</a>
      </div>
    </div>
  </div>
</footer>
