---
const { label, href, variant = 'primary' } = Astro.props;
---

<a
  href={href}
  class={'text-white px-8 py-4 rounded-md uppercase font-light' +
    (variant === 'primary' ? ' bg-secondary' : ' bg-primary')}>{label}</a
>
