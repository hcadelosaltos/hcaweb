---
import HomeServiceCard from './HomeServiceCard.astro';
import { homeServices } from '../data/homeServices';
---

<section
  class="bg-[url(/src/assets/marcaagua.png)] bg-no-repeat bg-left bg-contain h-[100vh]"
>
  <div class="bg-[#f5f5f5]/75 p-10 pb-20">
    <div
      class="container mx-auto flex flex-col items-center justify-center gap-10"
    >
      <img src="/src/assets/logo.png" alt="" />
      <p class="font-bold text-xl max-w-[50ch] text-center">
        Creemos que nadie debería estar limitado por su salud cuando tiene tanto
        por aportar al mundo. Por eso, ofrecemos atención médica integrada,
        personalizada y acecesible, con tecnologías que simplifican la vida del
        paciente, a través de servicios médicos quirúrgicos y hospitalarios de
        alta calidad.
      </p>
    </div>
  </div>
  <div
    class="p-10 flex gap-16 justify-between items-center container mx-auto -mt-20"
  >
    {
      homeServices.map((service) => (
        <HomeServiceCard
          title={service.title}
          description={service.description}
          alt={service.alt}
          image={service.image}
          label={service.label}
          href={service.href}
        />
      ))
    }
  </div>
</section>
